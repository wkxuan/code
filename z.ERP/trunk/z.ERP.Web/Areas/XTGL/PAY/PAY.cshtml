@using z.ERP.Web.Areas.Share.Render
@using z.ERP.Entities.Enum
@inherits z.MVC5.Views.DefineViewPage

@section element{
<row>
    <i-col span="2" class="RowTitle">
        代码
    </i-col>
    <i-col span="6">
        @{
            RenderControl(new TextBoxRender()
            {
                vModel = "dataParam.CODE",
                Id = "tb_code",
            });
        }
    </i-col>
    <i-col span="2" class="RowTitle">
        名称
    </i-col>
    <i-col span="6">
        @{
            RenderControl(new TextBoxRender()
            {
                vModel = "dataParam.NAME"
            });
        }
    </i-col>
    <i-col span="2" class="RowTitle">
        类型
    </i-col>
    <i-col span="6">
        @{
            RenderControl(new EnumDropDownListRender<支付方式类型>()
            {
                vModel = "dataParam.TYPE"
            });
        }
    </i-col>
</row>

}

@section list{
 <row>
     <i-table border :columns="dataParam.colPay" :data="dataParam.dataPay"></i-table>
 </row>
}
